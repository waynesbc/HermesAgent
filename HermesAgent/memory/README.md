# Memory

Use this folder for mutable state that Hermes should remember between runs.

- `project-state.json` for the current project focus and constraints
- `tasks.json` for active, completed, and blocked work
- `decisions.json` for durable choices and rationale

Keep this small and update it after meaningful work.
